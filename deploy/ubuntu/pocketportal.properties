# Safe initial Ubuntu-host configuration.
# Keep the service on localhost until authentication and private networking are configured.
server.host=127.0.0.1
server.port=8080
android.adb.path=/usr/bin/adb
android.adb.timeoutMillis=5000

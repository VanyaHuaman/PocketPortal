#!/usr/bin/env bash
set -euo pipefail

if [[ "${1:-}" == "--user" && "${2:-}" == "is-system-running" ]]; then
  echo "running"
  exit 0
fi

if [[ -n "${POCKETPORTAL_TEST_SERVICE_LOG:-}" ]]; then
  echo "$*" >>"$POCKETPORTAL_TEST_SERVICE_LOG"
fi
